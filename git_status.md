# Check the current status of the repository
git status

# Git Symbols in VS Code / Git terminal

# M = Modified → file changed 
# must be added first then commit

# U = Untracked → new file, not tracked by Git
# must be added first then commit

# A = Added → staged (git add done) and ready to commit