# After committing files using 'git commit', your changes are only local
# To update the remote GitHub repository, you must push the commits

# Push commits to the github repository 
git push origin main

# Push local commits to GitHub (first time)
git push -u origin main
# Next time push (shortcut)
git push